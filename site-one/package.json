{
  "name": "phpmytech-61-65-auth",
  "version": "1.0.0",
  "private": true,
  "description": "Защищённая авторизация для технического отдела 61-65",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check lib/database.js && node --check lib/passwords.js && node --check public/app.js && node --check public/workspace.js && node --check public/admin.js && node --check scripts/hash-password.js",
    "hash-password": "node scripts/hash-password.js"
  },
  "dependencies": {
    "mysql2": "^3.15.0"
  },
  "engines": {
    "node": ">=20"
  }
}
